#ifndef ROLL_DICE_H
#define ROLL_DICE_H

#include <string>

using namespace std;

void inputError(const string& errorMessage);
void menu();
void choice();
void rollDice();
void history();

#endif // ROLL_DICE_H